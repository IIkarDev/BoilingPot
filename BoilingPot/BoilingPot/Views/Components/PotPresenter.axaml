<!-- Views/Components/PotPresenter.axaml -->
<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:BoilingPot.ViewModels.Components" 
             x:Class="BoilingPot.Views.Components.PotPresenter"
             x:DataType="vm:IPotViewModel"> <!-- !!! Важно: DataContext - ИНТЕРФЕЙС !!! -->

    <Design.DataContext>
        <!-- Для предпросмотра можно подставить конкретную реализацию -->
        <vm:MainPotViewModel/>
    </Design.DataContext>

    <!-- Содержимое по умолчанию (будет заменено темой) -->
    <TextBlock Text="[Кастрюля]" Opacity="0.5"
               HorizontalAlignment="Center" VerticalAlignment="Center"/>

</UserControl>